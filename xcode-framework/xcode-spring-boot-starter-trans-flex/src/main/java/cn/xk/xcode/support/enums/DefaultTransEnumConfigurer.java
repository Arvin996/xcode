package cn.xk.xcode.support.enums;

public class DefaultTransEnumConfigurer implements TransEnumConfigurer{

    @Override
    public void registry(TransEnumsRegistry registry) {
        TransEnumConfigurer.super.registry(registry);
    }
}
